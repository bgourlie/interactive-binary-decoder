import * as React from "react";

export default class RouteNotFound extends React.PureComponent {
  render(): React.ReactNode {
    return (
      <section>
        <p>Where are you trying to go homie?</p>
      </section>
    );
  }
}
